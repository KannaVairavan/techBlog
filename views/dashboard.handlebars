{{#if postDetail.length}}
<section>
    <h2>Posts List</h2>
    {{#each postDetail as |post|}}
    {{>dashboard-postdetail post}}
    {{/each}}
</section>

{{/if}}

<button type="submit" class="btn btn-primary"><a class="link" href="/dashboard/new">+ New Post</a> </button>


{{!--
<script src="/js/createPost.js"></script> --}}