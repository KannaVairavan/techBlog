<div class="card">
    <div class="card-header">
        <a href="/post/{{id}}">
            <header>{{title}}</header>
            <p>Posted by {{user.username}} on {{format_date created_date}}</p>
    </div>

    <div class="card-body">
        {{content}}
    </div>
    </a>
</div>